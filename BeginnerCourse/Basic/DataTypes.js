/* Data Types (7):
    undefined, null, boolean, string, symbol, number, and object

    undefined: sth that hasn't been defined.
    null: set to be sth and that thing is nothing.
    symbol: a immutable primitive value that is unique.
    object: can store a lot of key value pairs.
*/

var myName = "Max"  // a var is able to be used throughout the whole program

myName = 8

let ourName = 'JSLearners'  // let will only be used within the scope of where you declare that

const pi = 3.14